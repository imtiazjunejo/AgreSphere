import { Camera, Mail, Phone, MapPin, Edit3, X } from "lucide-react";

const API_BASE_URL = "http://localhost:3000";

// This component is no longer used in the LinkedIn-style layout
// The profile card functionality is now integrated into ProfilePage.jsx
const ProfileCard = () => {
  return null;
};

export default ProfileCard;